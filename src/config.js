var config = {
  debug: true,
  mysql: {
    host: 'localhost',
    user: 'root',
    password: '',
    database: 'level_upgrade',
  },
  port: 5000,
  server: 'localhost',
}

module.exports = config
